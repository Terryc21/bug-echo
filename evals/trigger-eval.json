[
  {
    "query": "ok so i just fixed a thing in CardDetailView.swift where we were doing `try? context.fetch(descriptor)` and swallowing the error — swapped it for a do/catch that logs. pretty sure i've written that same try? fetch thing in a bunch of other places over the years. can you go look?",
    "should_trigger": true,
    "note": "Canonical case. Real fix just landed, user suspects siblings, never names the skill."
  },
  {
    "query": "just pushed a commit that fixes the retain cycle in NotificationCoordinator (was capturing self strongly in the observer closure). before i close the ticket i want to know if the same capture pattern is anywhere else in Sources/",
    "should_trigger": true,
    "note": "Fix + explicit scope + close-out intent. Should fire in inferred mode from the commit."
  },
  {
    "query": "i've now fixed this identical off-by-one in the pagination math three separate times in three different files this month. i'm done playing whack-a-mole — find me every remaining instance",
    "should_trigger": true,
    "note": "Frustration phrasing, no skill name, no git verb. The 'whack-a-mole' shape is exactly the bug-class problem."
  },
  {
    "query": "we shipped a hotfix last night for the crash where a SwiftData model was constructed with `let id = UUID()` inside a computed var feeding a ForEach. Stuffolio is ~600 files. can you sweep for the same shape and rate what you find so i know what's release-blocking for build 36?",
    "should_trigger": true,
    "note": "Multi-condition pattern + large repo + explicitly asks for rating. Exercises the described mode and the rating table."
  },
  {
    "query": "echo this fix",
    "should_trigger": true,
    "note": "Terse listed trigger phrase, no context. Tests that the short form still works."
  },
  {
    "query": "before I mark UNFORGET A67 as Fixed I want to generalize it — the fix replaced a per-participant .permission write with publicPermission on the CKShare. where else do we write per-participant permissions?",
    "should_trigger": true,
    "note": "Terry's actual workflow (unforget -> generalize). Domain-specific, describes what the fix replaced."
  },
  {
    "query": "fixed the force-unwrap crash in ExportService.swift:212. is this pattern anywhere else in the codebase or was it a one-off?",
    "should_trigger": true,
    "note": "Short, concrete, file:line. The 'or was it a one-off' framing is precisely the recon-scout 0-bucket question."
  },
  {
    "query": "my teammate fixed a bug on main last week where async work inside a forEach wasn't awaited — the loop finished before the writes did. we're on a react/typescript monorepo. can you find other places we do the same thing?",
    "should_trigger": true,
    "note": "Non-Swift, fix by someone else, older than HEAD. Tests language-agnostic claim and the described-mode fallback."
  },
  {
    "query": "audit this file for anything that might break in the future — i haven't seen a bug yet but this StoreKit transaction handler makes me nervous",
    "should_trigger": false,
    "note": "NEAR-MISS: forward-looking, no fix exists. This is bug-prospector's job. Over-triggering here strands bug-echo with nothing to infer from."
  },
  {
    "query": "find bugs in Sources/Features/Sharing/",
    "should_trigger": false,
    "note": "NEAR-MISS: shares 'find bugs' keyword but there is no pattern and no fix. Generic bug hunt, not a sibling sweep."
  },
  {
    "query": "the app crashes when i tap Share on iPad but not iPhone. figure out why",
    "should_trigger": false,
    "note": "NEAR-MISS: a bug, but an UNDIAGNOSED one. Root-cause work (/investigate), and bug-echo needs the fix to exist first."
  },
  {
    "query": "i just fixed the typo in the onboarding copy — 'recieve' to 'receive'. commit it for me",
    "should_trigger": false,
    "note": "NEAR-MISS: genuinely 'I just fixed X', but the skill's own Honest Limits say a wording change has no code shape to extract. Tests that 'just fixed' alone isn't enough."
  },
  {
    "query": "run swiftlint across the project and fix whatever it flags",
    "should_trigger": false,
    "note": "NEAR-MISS: pattern-matching across a codebase, but from a rule catalog, not from a fix. The README explicitly draws this line."
  },
  {
    "query": "review my PR before i merge it — 4 files changed, mostly the new AppleCare expiry logic",
    "should_trigger": false,
    "note": "NEAR-MISS: diff-based analysis of recent changes, which is /code-review or /review. bug-echo reads a diff to extract a pattern, not to judge the diff."
  },
  {
    "query": "we keep getting the same three support emails about photos not syncing. can you look at the sync code and tell me what's wrong",
    "should_trigger": false,
    "note": "NEAR-MISS: 'same thing repeating' phrasing that superficially resembles a bug class, but it's recurring SYMPTOMS with no fix and no identified pattern. Diagnosis, not generalization."
  },
  {
    "query": "add tests for the CKRecordMapper inventory field — we had a bug there in July that went undetected",
    "should_trigger": false,
    "note": "NEAR-MISS: references a past fixed bug, but the ask is test coverage. Tests that a mentioned past bug doesn't pull bug-echo in."
  },
  {
    "query": "i refactored ItemDetailView into three smaller views yesterday. does the same god-view problem exist elsewhere?",
    "should_trigger": true,
    "note": "EDGE: a refactor rather than a bug fix, but structurally identical — a shape was replaced and the user wants siblings. Should trigger; if it doesn't, the description is too narrowly keyed on the word 'bug'."
  },
  {
    "query": "what's the difference between bug-echo and bug-prospector? which one should i be running after a hotfix",
    "should_trigger": false,
    "note": "EDGE: names the skill directly but asks a META question. Answer in conversation; running a scan here would be wrong."
  },
  {
    "query": "scan for similar bugs",
    "should_trigger": true,
    "note": "Listed trigger phrase, bare. Included partly to check it isn't so vague it drags in generic bug-hunt queries."
  },
  {
    "query": "delete the old CloudKitLegacyImporter.swift file, we fixed the duplicate-syncID bug and it's dead code now",
    "should_trigger": false,
    "note": "NEAR-MISS: mentions a fixed bug AND a code sweep intent, but the ask is deletion. Strong negative — has more surface overlap than any other."
  }
]
